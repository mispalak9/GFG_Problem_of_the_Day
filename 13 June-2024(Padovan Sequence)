class Solution:
    def padovanSequence(self, n):
        mod = 10**9+7
        p = [0]*(n+1)
        p[0] = p[1] = p[2] = 1
        for i in range(3, n+1):
            p[i] = (p[i-2] + p[i-3]) % mod
        return p[n]
